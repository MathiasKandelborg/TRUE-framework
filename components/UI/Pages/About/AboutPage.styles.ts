import * as MUI from '@material-ui/core'

const aboutPageStyles = MUI.makeStyles((theme: MUI.Theme) =>
  MUI.createStyles({
    root: {
      padding: theme.spacing(2)
    }
  })
)

export default aboutPageStyles
