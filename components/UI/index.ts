import PageAnimation from './Animation/PageAnimation'
import TitleAnimation from './Animation/TitleAnimation'
import { Layout } from './Layout'
/**
 * Only export used components
 * E.g those that are used outside the `components` folder
 * This is to avoid too many import options
 */
// eslint-disable-next-line import/prefer-default-export
export { Layout, TitleAnimation, PageAnimation }
