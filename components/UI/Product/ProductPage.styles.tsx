import * as MUI from '@material-ui/core'

const singleProductPageStyles = MUI.makeStyles((theme: MUI.Theme) =>
  MUI.createStyles({
    paper: {
      padding: theme.spacing(2)
    }
  })
)

export default singleProductPageStyles
